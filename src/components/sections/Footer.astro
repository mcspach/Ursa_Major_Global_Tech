---
import { withBase } from "../../utils/paths";
import ConstellationMark from "../ui/ConstellationMark.astro";

const year = new Date().getFullYear();

const QUICK_LINKS = [
  { label: "Products", href: "/products/" },
  { label: "Services", href: "/services/" },
  { label: "Advisory", href: "/advisory/" },
  { label: "Blog", href: "/blog/" },
  { label: "About", href: "/about/" },
];
---

<footer class="site-footer">
  <div class="container container--wide">
    <div class="site-footer__grid">
      <div class="site-footer__brand">
        <a href={withBase("/")} class="site-footer__brand-link" aria-label="Ursa Major Global Tech, home">
          <ConstellationMark class="site-footer__mark" />
          <span class="site-footer__name">Ursa Major Global Tech</span>
        </a>
        <p class="site-footer__tagline">Bringing Frontier AI down to earth.</p>
        <p class="site-footer__blurb">
          We build MVPs, SaaS platforms, and custom applications with AI-accelerated
          development. Production-ready software, shipped in weeks.
        </p>
      </div>

      <nav class="site-footer__col" aria-label="Quick links">
        <h2 class="site-footer__heading">Quick Links</h2>
        <ul>
          {
            QUICK_LINKS.map((link) => (
              <li>
                <a href={withBase(link.href)}>{link.label}</a>
              </li>
            ))
          }
        </ul>
      </nav>

      <div class="site-footer__col">
        <h2 class="site-footer__heading">Contact</h2>
        <ul>
          <li><a href="mailto:hello@ursamajorglobaltech.com">hello@ursamajorglobaltech.com</a></li>
          <li><a href={withBase("/contact/")}>Book a Call</a></li>
          <li><a href={withBase("/contact/")}>Contact Us</a></li>
        </ul>
      </div>
    </div>

    <div class="site-footer__bottom">
      <p>© {year} Ursa Major Global Tech</p>
      <p class="site-footer__bottom-tag">Bringing Frontier AI down to earth.</p>
    </div>
  </div>
</footer>

<style lang="scss">
  .site-footer {
    position: relative;
    background: var(--color-bg-deep);
    border-top: 1px solid var(--color-border);
    padding-block: var(--space-2xl) var(--space-lg);
    overflow: hidden;
  }

  .site-footer__grid {
    display: grid;
    grid-template-columns: minmax(0, 1.6fr) minmax(0, 1fr) minmax(0, 1fr);
    gap: var(--space-xl);
  }

  .site-footer__brand-link {
    display: inline-flex;
    align-items: center;
    gap: 0.875rem;
  }

  .site-footer__mark {
    width: 2.75rem;
    height: auto;
    color: var(--peri-400);

    :global(.cstar) {
      fill: var(--star-100);
    }

    :global(.cline) {
      stroke: rgba(165, 175, 251, 0.55);
    }
  }

  .site-footer__name {
    font-family: var(--font-display);
    font-size: var(--text-md);
    font-weight: 590;
    color: var(--color-text-strong);
  }

  .site-footer__tagline {
    margin-top: var(--space-md);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--peri-400);
  }

  .site-footer__blurb {
    margin-top: var(--space-sm);
    max-width: 34ch;
    font-size: var(--text-sm);
    color: var(--color-text-subtle);
  }

  .site-footer__heading {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-text-strong);
    margin-bottom: var(--space-md);
  }

  .site-footer__col ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;

    a {
      font-size: var(--text-sm);
      color: var(--color-text-muted);

      &:hover {
        color: var(--color-link);
      }
    }
  }

  .site-footer__bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-sm);
    margin-top: var(--space-2xl);
    padding-top: var(--space-lg);
    border-top: 1px solid var(--color-border);

    p {
      font-family: var(--font-mono);
      font-size: var(--text-xs);
      color: var(--color-text-subtle);
    }
  }

  @media (max-width: 800px) {
    .site-footer__grid {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }

    .site-footer__bottom {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
